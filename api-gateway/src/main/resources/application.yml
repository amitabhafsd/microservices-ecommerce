
spring:
  application:
    name: API-GATEWAY

  config:
    import: optional:configserver:http://localhost:8888

  cloud:
    config:
      name: api-gateway

jwt:
  secret: ${JWT_SECRET:mysecretkeymysecretkeymysecretkey}
